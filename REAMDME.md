# Setting Up the Development Environment

To set up the development environment for this project in one command, run:

```bash
python3 -m venv venv && source venv/bin/activate && pip install -r requirements-dev.in
```
